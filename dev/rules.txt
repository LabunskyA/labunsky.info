${PAGE_SHORT}
cat dev/short.html

${PAGE_LONG}
cat dev/long.html

    ${META}
    cat dev/meta.html

        ${TITLE}
        echo -n $(grep -w -Po 'title=\K[^\n]*' < "{:s}/{:s}.conf")

        ${DESCRIPTION}
        echo -n $(grep -w -Po 'description=\K[^\n]*' < "{:s}/{:s}.conf")

    ${STYLES}
    cat "{:s}/{:s}.css" 2>/dev/null

    ${LANG_SWITCHER}
    cat dev/lang_switcher.html

    ${MIRROR}
    echo -n $(grep -w -Po 'mirror=\K[^\n]*' < "{:s}/{:s}.conf")

    ${CONTENT}
    echo -n $(grep -w -Po 'content=\K[^\n]*' < "{:s}/{:s}.conf")

        ${TXT}
        cat "{:s}/{:s}.txt"

        # markdown subtype
        ${MD}
        pandoc --from gfm --to html "{:s}/{:s}.md"

    ${FOOTER}
    cat dev/footer.html