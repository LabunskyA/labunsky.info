${PAGE_SHORT}
cat dev/short.html

${PAGE_LONG}
cat dev/long.html

    ${META}
    cat dev/meta.html

        ${LANG}
        echo -n $(grep -w -Po 'lang=\K[^\n]*' < "{:s}/{:s}.conf")

        ${TITLE}
        echo -n $(grep -w -Po 'title=\K[^\n]*' < "{:s}/{:s}.conf")

        ${DESCRIPTION}
        echo -n $(grep -w -Po 'description=\K[^\n]*' < "{:s}/{:s}.conf")

    ${STYLES}
    cat "{:s}/{:s}.css" 2>/dev/null

    ${CONTENT}
    echo -n $(grep -w -Po 'content=\K[^\n]*' < "{:s}/{:s}.conf")

        ${TXT}
        cat "{:s}/{:s}.txt"

        # markdown subtype
        ${MD}
        pandoc --from gfm --to html "{:s}/{:s}.md"

    ${FOOTER}
    cat dev/footer.html